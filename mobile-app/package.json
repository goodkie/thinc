{
  "name": "the-truth-untold-mobile",
  "version": "1.5.27",
  "description": "Th!nc Biometric Lie Detector — Mobile Edition (Capacitor)",
  "private": true,
  "scripts": {
    "build": "echo 'Copy www assets then run: npx cap sync'",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}